# Local Data Layer

Local JSON data lives in `/data` for public structured content and can later map to external storage such as Supabase.

Future Supabase mapping:

- JSON records become database seed data.
- Stable keys become slugs or IDs.
- Public image slots can map to storage object paths.
